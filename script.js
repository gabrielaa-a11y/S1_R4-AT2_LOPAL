let altura = prompt("Digite sua altura em metros:");  // prompt pede a altura, let guarda o valor

if (altura >= 1.40) {         //if verifica
    alert("Pode entrar no brinquedo.");       //alert mostra a mensagem
} else {
    alert("Altura insuficiente.");
}